framework:
    rate_limiter:
        booking_per_hour:
            policy: 'sliding_window'
            limit: 5
            interval: '1 hour'
        login_attempt:
            policy: 'sliding_window'
            limit: 5
            interval: '15 minutes'
